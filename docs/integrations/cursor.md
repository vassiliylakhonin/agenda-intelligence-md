# Cursor Integration

Cursor can run shell commands from a cell. To use the CLI:
```bash
!agenda-intelligence source-plan technology-ai
```
Capture the JSON output and feed it back to the model for further processing.
