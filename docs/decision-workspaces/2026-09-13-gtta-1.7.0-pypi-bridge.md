# Decision workspace: GTTA 1.7.0 PyPI bridge

- **Goal:** publish the already tagged `global-think-tank-analyst==1.7.0`
  distributions to its declared public PyPI channel.
- **Trusted evidence:** GTTA commit `c65e404` is tagged `v1.7.0`; its local
  release gate passed; GitHub CI run `34751494062` passed all jobs; PyPI did
  not contain `1.7.0` before dispatch; the same repository-scoped bridge
  pattern published GTTA `1.6.0` successfully from commit `71d337e` and was
  removed in `df93e17`.
- **Suspected unreliable evidence:** the sibling repository cannot reveal
  whether `PYPI_API_TOKEN` remains valid because GitHub secrets are
  write-only. Its validity is established only by the upload result and PyPI
  readback.
- **Hidden assumptions:** the retained token is still account-scoped and has
  permission to publish the existing GTTA project, as it did for `1.6.0`.
- **Intended next action:** manually dispatch the hard-coded workflow once;
  it checks out only GTTA `v1.7.0`, verifies exact project name and version,
  repeats tests, repository checks, distribution metadata checks, and the
  installed-wheel smoke test, then uploads.
- **Stop or escalate if:** the tag or package metadata differs; any gate fails;
  PyPI already contains `1.7.0`; the token is rejected; or post-upload clean
  install does not report `gtta.__version__ == "1.7.0"`.
- **Cleanup:** after PyPI JSON and clean-install verification, delete the
  temporary workflow. Retain this record as the audit trail; do not retain a
  standing cross-project publication path.

## Outcome

- Workflow run
  [`34752106521`](https://github.com/vassiliylakhonin/agenda-intelligence-md/actions/runs/34752106521)
  completed successfully after all declared gates.
- PyPI accepted the wheel at `2026-09-13T10:36:46.596975Z` with SHA-256
  `656f8253dd610a73a588a5b00f10851e0492bef73bf4abdb4afca8c0ccc6db2c`.
- PyPI accepted the sdist at `2026-09-13T10:36:47.755711Z` with SHA-256
  `d6928527059793432af5a92ca4d727f02fd774991d0fd3b1139ff52dae4fa048`.
- A clean Python 3.12 environment installed
  `global-think-tank-analyst[verification]==1.7.0` from PyPI and confirmed
  `gtta.__version__ == "1.7.0"`, `agenda_intelligence.__version__ == "1.9.0"`,
  and the installed `artifact-schema` and `source-catalog-schema` CLI surfaces.
- The temporary workflow was removed immediately after verification. No token
  was copied into GTTA and no standing cross-project publisher was retained.
