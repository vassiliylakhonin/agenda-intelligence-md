name = "agenda-intelligence-a2a"
main = "src/index.js"
compatibility_date = "2026-05-22"
workers_dev = true

[observability]
enabled = true

[[kv_namespaces]]
binding = "AGENDA_USAGE"
id = "d2228183999246ccbec8aa2d5f130bfc"

# Optional production access gate. When the MIDDLE_CORRIDOR_API_KEY secret is
# set, the message/send production route requires `Authorization: Bearer <key>`
# (401 + WWW-Authenticate: Bearer otherwise) and the agent card advertises the
# `productionBearer` security requirement. While unset, the route stays an open
# free demo and the card advertises no requirement (current state). To gate it:
#   wrangler secret put MIDDLE_CORRIDOR_API_KEY --env middle-corridor-deal-risk-gate
# agent/card discovery stays public regardless.
[env.middle-corridor-deal-risk-gate]
name = "middle-corridor-deal-risk-gate-a2a"
workers_dev = true

[env.middle-corridor-deal-risk-gate.vars]
AGENT_PROFILE = "kazakhstan"

[[env.middle-corridor-deal-risk-gate.kv_namespaces]]
binding = "AGENDA_USAGE"
id = "d2228183999246ccbec8aa2d5f130bfc"

# CIS Secondary-Sanctions Exposure — per ADR 0014, this profile declares
# optional live sanctions-list retrieval. Watchman self-host is preferred
# (Apache-2.0, no per-call fee); OpenSanctions hosted API remains a paid
# fallback (CC-BY-4.0). Before deploying with activation:
#   wrangler secret put WATCHMAN_URL --env cis-secondary-sanctions
# or:
#   wrangler secret put OPENSANCTIONS_API_KEY --env cis-secondary-sanctions
# Without either secret, the profile responds gracefully with
# live_retrieval_status: disabled and triage based on user-supplied evidence.
[env.cis-secondary-sanctions]
name = "cis-secondary-sanctions-a2a"
workers_dev = true

[env.cis-secondary-sanctions.vars]

AGENT_PROFILE = "cis_secondary_sanctions"
# Snapshot upstream (ADR 0014, activated by ADR 0020): $0 server-side name
# screening against a compact public-list index served over HTTP. Not a secret
# (public URL), so it belongs in [vars] rather than a secret.
#
# The index is built by scripts/sanctions_name_index.py and published to
# Cloudflare Pages from deploy/snapshot-site — see that directory's README for
# the rebuild-and-republish command. It is a snapshot, so its freshness is
# whatever generated_at_utc says; the worker reports that date back to callers.
# Set SNAPSHOT_DISABLED=1 to deactivate without removing the URL — the adapter
# then reports live_retrieval_status "disabled" and falls back to caller-supplied
# evidence. It never reports an unscreened name as clean.
SNAPSHOT_INDEX_URL = "https://sanctions-name-index.pages.dev/sanctions-name-index-compact.json"

# Optional access controls for the message/send route — both OFF by default so
# the browser demo stays an open free evaluation. Flip on when needed:
#   Hard paywall (closes the route, incl. the demo) — issue a key per customer:
#     wrangler secret put CIS_SECONDARY_SANCTIONS_API_KEY --env cis-secondary-sanctions
#   Soft per-IP/hour throttle (keeps the demo usable, caps bulk free use) —
#     uncomment the line below (best-effort, KV-backed, fails open):
# RATE_LIMIT_PER_HOUR = "60"

[[env.cis-secondary-sanctions.kv_namespaces]]
binding = "AGENDA_USAGE"
id = "d2228183999246ccbec8aa2d5f130bfc"

[env.agentic-interaction-trust]
name = "agentic-interaction-trust-a2a"
workers_dev = true

[env.agentic-interaction-trust.vars]
AGENT_PROFILE = "agentic_interaction_trust"

[[env.agentic-interaction-trust.kv_namespaces]]
binding = "AGENDA_USAGE"
id = "d2228183999246ccbec8aa2d5f130bfc"

# Agent Output Verification — relay-readiness triage of another agent's claim set.
# Evidence-readiness only, no live retrieval. Open free demo (no API key). Deploy:
#   wrangler deploy --env agent-output-verification
[env.agent-output-verification]
name = "agent-output-verification-a2a"
workers_dev = true

[env.agent-output-verification.vars]
AGENT_PROFILE = "agent_output_verification"

[[env.agent-output-verification.kv_namespaces]]
binding = "AGENDA_USAGE"
id = "d2228183999246ccbec8aa2d5f130bfc"

[env.gulf-maritime-exposure]
name = "gulf-maritime-exposure-a2a"
workers_dev = true

[env.gulf-maritime-exposure.vars]
AGENT_PROFILE = "gulf_maritime_exposure"

[[env.gulf-maritime-exposure.kv_namespaces]]
binding = "AGENDA_USAGE"
id = "d2228183999246ccbec8aa2d5f130bfc"

# Kazakhstan Market-Entry Readiness Gate — evidence triage only, no live
# retrieval. Open free demo (no API key). Deploy with:
#   wrangler deploy --env kazakhstan-market-entry-readiness
[env.kazakhstan-market-entry-readiness]
name = "kazakhstan-market-entry-readiness-a2a"
workers_dev = true

[env.kazakhstan-market-entry-readiness.vars]
AGENT_PROFILE = "market_entry_readiness"

[[env.kazakhstan-market-entry-readiness.kv_namespaces]]
binding = "AGENDA_USAGE"
id = "d2228183999246ccbec8aa2d5f130bfc"

# Corridor & Sanctions Risk Assistant — lightweight discovery FRONT (Zee-pattern),
# not a vertical worker. Orients a human, routes to the structured gates, and
# hands off to person-led work after fit, scope, fee, and timing are confirmed.
# No triage, scoring, screening, or retrieval of its own. Open free demo (no API key). Deploy with:
#   wrangler deploy --env corridor-sanctions-assistant
[env.corridor-sanctions-assistant]
name = "corridor-sanctions-assistant-a2a"
workers_dev = true

[env.corridor-sanctions-assistant.vars]
AGENT_PROFILE = "corridor_sanctions_assistant"

[[env.corridor-sanctions-assistant.kv_namespaces]]
binding = "AGENDA_USAGE"
id = "d2228183999246ccbec8aa2d5f130bfc"

# Critical Minerals & Strategic Raw Materials Due Diligence Gate — origin tracing and CSDDD compliance.
# Evidence triage only, no live retrieval. Open free demo (no API key). Deploy with:
#   wrangler deploy --env critical-minerals-due-diligence
[env.critical-minerals-due-diligence]
name = "critical-minerals-due-diligence-a2a"
workers_dev = true

[env.critical-minerals-due-diligence.vars]
AGENT_PROFILE = "critical_minerals_due_diligence"

[[env.critical-minerals-due-diligence.kv_namespaces]]
binding = "AGENDA_USAGE"
id = "d2228183999246ccbec8aa2d5f130bfc"


# Dual-Use Technology & Export Controls Gate - ECCN/HS Code screening.
# Evidence triage only, no live retrieval. Open free demo (no API key). Deploy with:
#   wrangler deploy --env dual-use-technology-export
[env.dual-use-technology-export]
name = "dual-use-technology-export-a2a"
workers_dev = true

[env.dual-use-technology-export.vars]
AGENT_PROFILE = "dual_use_technology_export"

[[env.dual-use-technology-export.kv_namespaces]]
binding = "AGENDA_USAGE"
id = "d2228183999246ccbec8aa2d5f130bfc"

# Sampled traces on the one profile that makes outbound calls. Measured
# 2026-08-29 over 22-28 Aug: this Worker is 12.4% of named-script requests but
# 47.4% of the fleet's CPU, with cpuTimeP99 423ms against a 1.27ms median, and
# it is the only agent in the fleet reporting subrequests (49; every other
# agent reports 0). The tail is therefore an outbound-call path, not
# computation, and traces name the fetch. Logs stay unsampled fleet-wide: at
# ~5,900 requests a week the free 200k/day budget is nowhere near binding, and
# thinning them would only cost the funnel its census.
#
# Re-measured 2026-09-02 over 26 Aug - 2 Sep, after fleet traffic grew 5.6x to
# 33,121 named-script requests a week: the reason to trace holds, the tail that
# set the rate does not. Still the CPU outlier (16.7% of fleet CPU on 9.0% of
# requests) and still the only agent reporting subrequests (23), but cpuTimeP99
# has collapsed from 423ms to 11.8ms — 36x — so the sampled 1-in-10 no longer
# catches a tail worth naming. Before touching head_sampling_rate, re-measure:
# a rate chosen for a 423ms tail is not a rate chosen for this one. The
# unsampled-logs argument survives the growth with room to spare — 33k a week
# is ~4.7k a day against the same 200k/day budget.
[env.cis-secondary-sanctions.observability]
enabled = true

[env.cis-secondary-sanctions.observability.traces]
enabled = true
head_sampling_rate = 0.1
